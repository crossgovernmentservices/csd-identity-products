---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: openam
  labels:
    app: openam
spec:
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: openam
        tier: web
    spec:
      containers:
        - name: openam
          image: forgerock/openam:latest
          ports:
            - containerPort: 8080
            - containerPort: 8443


---
apiVersion: v1
kind: Service
metadata:
  name: openam
  labels:
    app: openam
spec:
  ports:
    - port: 443
      targetPort: 8443
  selector:
    app: openam
    tier: web
  type: LoadBalancer
